WFMHUB CLEAN DATA FEED

WFMHub writes clean exports here when you choose "Export clean data".
Each dataset gets its own folder and includes a manifest beside the export.

The original FTE, Storm, and Verint extracts are never moved or changed.
PCS Performance.xlsx does not depend on this folder; it is generated directly
by Python and SQLite like the other finished reports.
