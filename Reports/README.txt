WFMHub publishes every current report in this folder using a fixed name.

PCS Team.xlsx is the only persistent team workbook. WFMHub refreshes its
Power Query source files under _system without replacing the workbook.

All other workbooks are replaceable snapshots. Before WFMHub replaces one,
the previous copy is saved under Reports\Archive\YYYY-MM-DD.
