# Manual input

The hub creates `correction_actions.xlsx` here during setup. Keep manual
decisions in that workbook, or edit the blue decision columns in an exported
report and use **Import correction decisions** from `WFMHub.cmd`.

Never store manual decisions inside an extract. Extracts remain untouched.
