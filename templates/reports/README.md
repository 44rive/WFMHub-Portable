# Report template policy

The Hub creates the first working report from the current prepared data. Stable
CSV feeds under `Feed/PCS` and `Feed/Absenteeism` can then drive long-lived
shared workbooks through a one-time Power Query connection. Previous report
copies are stored under `Reports/Archive` before replacement.
