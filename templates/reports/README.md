# Report template policy

Current report workbooks are generated directly by Python and SQLite. There is
no Excel master, Power Query, or Data Model setup. Previous generated copies are
stored under `Reports/Archive` before replacement.
