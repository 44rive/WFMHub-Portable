# FTE standard template

Copy `FTE Count.xlsx` to the configured `FTE` source folder. Maintain agent
rows only on its `Agent` sheet and keep the provided headers unchanged.

The workbook is intentionally blank and contains no employee data. `Client ID`
and `Name` are required; the other columns enrich reports and roster matching.
