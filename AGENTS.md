# WFMHub agent instructions

Before changing this repository, read `AI_CONTEXT.md`, then
`docs/REPORT_DESIGN_SYSTEM.md` and the domain document named in its authoritative
file index. The context file defines source roles, report maturity and
anti-hallucination rules.

Do not infer queue membership, revive retired AP sources, use Verint Activities
as attendance evidence, average rates, invent KPI targets, edit extracts, or
change fixed Excel sheet/table/key contracts without an explicit migration.

Run `PYTHONPATH=src:. pytest -q` and validate generated XLSX archives before
claiming completion.
