# WFMHub repository context

The canonical project handoff is `AI_CONTEXT.md`. Read it completely before
analysis or code changes. The approved workbook presentation contract is
`docs/REPORT_DESIGN_SYSTEM.md`.

Treat `src/wfmhub/report_packs.py`, effective installed configuration, tests and
the authority hierarchy in `AI_CONTEXT.md` as current. TOLEARN and legacy code
are evidence only, never permission to restore retired behavior.
