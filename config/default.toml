[app]
timezone = "Europe/Berlin"

[paths]
# Relative paths are resolved from the WFMHub folder.
source_root = "extracts"
database = "database/wfm.sqlite3"
output = "output"
logs = "logs"
backups = "backups"
input = "input"
custom = "custom"
business_rules = "config/wfm_rules.toml"

[sources]
# FTE Agent is the authoritative "our agents" roster. Schedule, LILO, and
# Agent Status rows are admitted by exact Agent ID or one unique normalized
# roster-name match. Source files are never changed.
fte_file = "FTE/FTE Count.xlsx"
lilo_folder = "Storm/LILO"
agent_status_folder = "Storm/Agent Status"
schedule_folder = "Verint/Schedules & Activities"
forecast_folder = "Verint/Forecast"
apbe_folder = "Storm/APBE ALL WFM"
apfr_folder = "Storm/APFR KPI SUIVI JOUR"
apde_folder = "Storm/APDE Standard KPIs Inbound Calls"
call_folder = "Storm/Call by Call"

[period]
# Leave blank to use every date currently available in the database.
start = ""
end = ""

[rules]
# Legacy status-compatibility settings. Attendance/absence tolerance and all
# active KPI formulas live in config/wfm_rules.toml.
tolerance_minutes = 5
break_minutes = 30
lunch_minutes = 45
minimum_status_coverage = 0.80
rta_stale_minutes = 30

[modules]
attendance = true
# Disabled by default: the SOTA hub does not calculate adherence. Enable only
# if you still need the legacy RTA/status compatibility output.
agent_status = false
intraday = true
forecast = true
pcs = true
absence = true

[pcs]
# Legacy compatibility copy. v0.4 reads the authoritative PCS calculation
# settings from config/wfm_rules.toml so every business rule is cataloged.
scored_questions = [1, 2]
comment_questions = [3]
survey_mode = "2"
minimum_score = 1
maximum_score = 5
top_box_minimum = 4
low_score_maximum = 2

[report]
max_attendance_rows = 100000
max_gap_rows = 100000
max_rta_rows = 10000
max_intraday_rows = 100000
max_pcs_agent_rows = 100000
max_pcs_exception_rows = 100000
max_absence_rows = 100000
max_service_rows = 100000

[report_packs]
# Keep outputs separated by business grain and audience. Quality PCS will use
# its own folder when the call-by-call source is added.
operations = "operations"
intraday = "intraday"
quality_pcs = "quality_pcs"
absence = "absence"
scorecard = "scorecard"
